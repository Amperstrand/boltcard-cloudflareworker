{
  "name": "boltcard-poc",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "devDependencies": {
    "axios": "^1.8.1",
    "jest": "^29.7.0",
    "jest-github-actions-reporter": "^1.0.3",
    "miniflare": "^4.20260116.0",
    "wrangler": "^4.59.3"
  },
  "dependencies": {
    "aes-js": "^3.1.2",
    "itty-router": "^5.0.23"
  },
  "scripts": {
    "prebuild": "node scripts/build_keys.js",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest --config=jest.config.js",
    "deploy": "npm test && node scripts/build_keys.js && wrangler deploy"
  }
}
